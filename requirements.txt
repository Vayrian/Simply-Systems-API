# simply-plural-backend/requirements.txt
Flask==3.0.3
Flask-SQLAlchemy==3.1.1
Flask-JWT-Extended==4.6.0
Flask-CORS==5.0.0
Flask-SocketIO==5.3.6
python-socketio==5.11.4
bcrypt==4.2.0
python-dotenv==1.0.1
Flask-Migrate==4.1.0
alembic==1.13.1

# Production server
gunicorn==22.0.0

# PostgreSQL driver
psycopg2-binary==2.9.9

# SocketIO async (use eventlet 0.33.3 or gevent for Python 3.12 compatibility)
eventlet==0.33.3           # <-- pinned to stable version that works with 3.12
# gevent==24.2.1           # <-- alternative (uncomment if eventlet fails)

# Optional – for stability
Werkzeug==3.0.3